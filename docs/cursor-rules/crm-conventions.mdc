---
description: CRM and consumer-app rules — how to consume @pythonidaer/ui correctly without overriding the design system.
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# CRM / Consumer App Conventions

## Always Import from the Package

```tsx
// ❌ BAD
import { Button } from '../../../packages/ui/src/components/Button';

// ✅ GOOD
import { Button } from '@pythonidaer/ui';
```

## Load Styles at App Root

```tsx
// src/main.tsx — must be first CSS import
import '@pythonidaer/ui/styles.css';
// or: import '@pythonidaer/tokens/tokens.css';
```

## Never Redefine Design System Components

Do not re-create Button, Card, Input, or any component already in `@pythonidaer/ui`. Extend via composition:

```tsx
// ❌ BAD — reinventing the wheel
function CrmButton() {
  return <button className="crm-btn" style={{ background: '#2563EB' }}>...</button>;
}

// ✅ GOOD — compose
function DealActionButton({ dealId }: { dealId: string }) {
  return <Button variant="primary" onClick={() => openDeal(dealId)}>Open Deal</Button>;
}
```

## Never Override Token Colors

```css
/* ❌ BAD */
:root {
  --color-primary-500: #FF0000;
}

/* ✅ GOOD — add CRM-specific tokens if needed */
:root {
  --crm-deal-won: var(--color-success-600);
  --crm-deal-lost: var(--color-error-600);
}
```

## Page Layout

Use CSS Modules with token variables only. No inline `style=` on JSX.
